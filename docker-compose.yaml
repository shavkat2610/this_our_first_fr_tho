services:
  frontend:
    build:
      context: ./angular
      dockerfile: Dockerfile
    ports:
      - "443:443"
    networks:
      - public
    depends_on:
      - backend

  db:
    # We use a mariadb image which supports both amd64 & arm64 architecture
    image: mariadb:10.6.4-focal
    # If you really want to use MySQL, uncomment the following line
    #image: mysql:8.0.27
    command: '--default-authentication-plugin=mysql_native_password'
    restart: always
    secrets:
      - db-password
    volumes:
      - type: bind
        source: /mnt/this_our_first_fr_tho/db/data
        target: /var/lib/mysql
    networks:
      - private
    environment:
      - MYSQL_DATABASE=example
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db-password
    ports:
      - "3306:3306"

  backend:
    build:
      args:
      - NODE_ENV=production
      context: expressJS
      dockerfile: Dockerfile
    command: npm run start
    environment:
      - DATABASE_DB=example
      - DATABASE_USER=root
      - DATABASE_PASSWORD=/run/secrets/db-password
      - DATABASE_HOST=db
      - DATABASE_PORT=3306
      - NODE_ENV=production
    ports:
      - 80:80
      - 9229:9229
      - 9230:9230
    secrets:
      - db-password
    volumes:
      - ./expressJS/src:/code/src:ro
      - ./expressJS/package.json:/code/package.json
      - ./expressJS/package-lock.json:/code/package-lock.json
      - back-notused:/opt/app/node_modules
    networks:
      - public
      - private
    depends_on:
      - db

networks:
  public:
    driver: bridge
  private:
    driver: bridge

volumes:
  back-notused:

secrets:
  db-password:
    file: db/password.txt